# Normalize line endings to LF in the repository on all platforms
* text=auto eol=lf

# PowerShell scripts — always LF in repo, Windows checks out CRLF
*.ps1 text eol=crlf

# Binary files — no line ending conversion
*.db binary
uv.lock text eol=lf
